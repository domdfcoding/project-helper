{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "additional_ignore": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of additional entries for ``.gitignore``."
    },
    "assignee": {
      "type": "string",
      "description": "The username of the GitHub account to assign issues to."
    },
    "author": {
      "type": "string",
      "description": "The name of the package author."
    },
    "email": {
      "type": "string",
      "description": "The email address of the author or maintainer."
    },
    "exclude_files": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of files not to manage with `project_helper`."
    },
    "imgbot_ignore": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of additional glob ignores for imgbot."
    },
    "mypy_deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of additional packages to install in Tox when running mypy. Usually type stubs."
    },
    "mypy_plugins": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of plugins to enable for mypy."
    },
    "mypy_version": {
      "type": [
        "string",
        "number"
      ],
      "description": "The version of ``mypy`` to use."
    },
    "on_github": {
      "type": [
        "boolean",
        "string"
      ],
      "description": "Whether this project is on GitHub."
    },
    "pre_commit_exclude": {
      "type": "string",
      "description": "Regular expression for files that should not be checked by pre_commit."
    },
    "repo_name": {
      "type": "string",
      "description": "The name of GitHub repository."
    },
    "source_files": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of source files belonging to the project, relative to the project root."
    },
    "tox_unmanaged": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of section names in ``tox.ini`` which should not be managed by ``project-helper``."
    },
    "username": {
      "type": "string",
      "description": "The username of the GitHub account hosting the repository."
    },
    "yapf_exclude": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of regular expressions to use to exclude files and directories from autoformatting."
    }
  },
  "required": [
    "author",
    "email"
  ],
  "additionalProperties": false
}
